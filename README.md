# 뮤타블랩스 D반 Python부 자료

```
(26/01/14) ver.1
```

- 다음 실행:
```bash
python -m venv .venv        # 가상환경 만들기
/.venv/Scripts/activate.ps1 # Windows 에서 가상환경 켜기
source /.venv/bin/activate  # Linux, MacOS 에서 가상환경 켜기
pip install -r requirements.txt # 필요한 라이브러리 설치
deactivate # 다 끝나고 가상환경 나가기
```